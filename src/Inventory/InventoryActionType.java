package Inventory;

// Enum class show available action type in inventory interface
public enum InventoryActionType {
    EQUIP_WEAPON,
    EQUIP_ARMOR,
    USE_POTION,
    CLOSE,
    INVENTORY,
    MAP,
    INFO,
    HELP,
    QUIT
}
